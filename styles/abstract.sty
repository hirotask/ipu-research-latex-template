%%岩手県立大学ソフトウエア情報学部
%%卒業論文要旨用スタイルファイル ver. 0.2
%%作成：Jan.23, 2002 ソフトウエア情報学部教務TF

%%%ページ番号表示等なし%%%
\pagestyle{empty}

%%%マージンの設定%%%%
%%A4: 210*297
\setlength{\textheight}{247mm}
\setlength{\oddsidemargin}{-0.5mm}
\setlength{\evensidemargin}{-0.5mm}
\setlength{\textwidth}{162mm}
\setlength{\columnsep}{8mm}
\setlength{\topskip}{0mm}
\setlength{\topmargin}{0mm}
\setlength{\headheight}{0mm}
\setlength{\headsep}{0mm}
\setlength{\voffset}{-0.4mm}


%%%節の表示設定%%%
\def\thesection {\arabic{section}.}
\def\thesubsection {\thesection\arabic{subsection}.}
\def\thesubsubsection {\thesubsection\arabic{subsubsection}}
\def\section{\@startsection{section}{1}{0pt}
   {12pt plus 1pt minus 1pt}{6pt plus 1pt minus 1pt}{\large\bf}}
\def\subsection{\@startsection{subsection}{2}{0pt}
   {4pt plus 1pt minus 1pt}{4pt plus 1pt minus 1pt}%
   {\normalsize\bf}}
\def\subsubsection{\@startsection{subsubsection}{3}{0pt}
   {.01pt plus .5pt minus .5pt}{.01pt plus .5pt minus .5pt}%
   {\normalsize\bf}}

%%%タイトルの変更要素指定＆２段組指定%%%
\def\maketitle{\par
 \begingroup
 \twocolumn[\@maketitle]
 \endgroup
 \let\maketitle\relax
 \let\@maketitle\relax
 \gdef\@title{}
 \gdef\@author{}
}

%%%新しいタイトル要素の指定%%%
\def\and{\hspace{1em}}
\def\etitle#1{\gdef\@etitle{\large #1}}
\def\lab#1{\gdef\@lab{#1}}
\def\studentid#1{\gdef\@studentid{#1}}
\def\teacher#1{\gdef\@teacher{#1}}
\def\name#1{#1}

%%%タイトルの表示設定%%%
\def\@maketitle{\newpage
 \null
{\centering
  {\Large
  {\renewcommand{\arraystretch}{.9}
  \begin{tabular}[t]{c}\bf{\@title} \\ \@etitle
  \end{tabular}\par}
  }
}
{\flushright
  \vskip1mm
  {\large
  {\renewcommand{\arraystretch}{.7}
  \begin{tabular}[t]{r}\@lab\hspace{1em}\@studentid\hspace{1em}\@author
  \end{tabular}\par}
  }
  \vskip1mm
  {\large
  {\renewcommand{\arraystretch}{.7}
  \begin{tabular}[t]{r}指導教員：\@teacher
  \end{tabular}\par}
  }
  \vskip7mm
}
}

%%%図表のキャプションの設定%%%

\long\def\@makecaption#1#2{
  \vskip\abovecaptionskip
  \iftdir\sbox\@tempboxa{#1\hskip1zw#2}%
    \else\sbox\@tempboxa{#1~ #2}%
  \fi
  \ifdim \wd\@tempboxa >\hsize%
    \iftdir #1\hskip1zw#2\relax\par
      \else #1~ #2\relax\par\fi%
  \else
    \global \@minipagefalse
    \hbox to\hsize{\hfil\box\@tempboxa\hfil}
  \fi
  \vskip\belowcaptionskip}

%%%参考文献の表示設定%%%
%
%\def\@cite#1{[#1]}

\def\@biblabel#1{\inhibitglue #1）\inhibitglue}
\renewenvironment{thebibliography}[1]
{\section*{\refname\@mkboth{\refname}{\refname}}%
   \list{\@biblabel{\@arabic\c@enumiv}}%
        {\settowidth\labelwidth{\@biblabel{#1}}%
         \leftmargin\labelwidth
         \advance\leftmargin\labelsep
         \setlength\parsep{0cm}
         \setlength\itemsep{0cm}
         \@openbib@code
         \usecounter{enumiv}%
         \let\p@enumiv\@empty
         \renewcommand\theenumiv{\@arabic\c@enumiv}}%
   \sloppy
   \clubpenalty4000
   \@clubpenalty\clubpenalty
   \widowpenalty4000%
   \sfcode`\.\@m}
  {\def\@noitemerr
    {\@latex@warning{Empty `thebibliography' environment}}%
   \endlist}
\let\@openbib@code\@empty

\def\@cite#1#2{$^{\mbox{\scriptsize{#1\if@tempswa,#2\fi})}}$}
%\def\@cite#1#2{$^{\mbox{\tiny{#1\if@tempswa , #2\fi})}}$}
